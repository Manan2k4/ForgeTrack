[project]
name = "forgetrack-backend"

[service.backend]
root = "backend"
start = "node server.js"
healthcheck.path = "/api/health"
healthcheck.interval = 10
healthcheck.timeout = 5
env.NODE_ENV = "production"

# Set in Railway dashboard: MONGODB_URI, JWT_SECRET, FRONTEND_URLS
